<%= link_to 'All posts', root_path, class: 'link'  %>

<div class="post--edit">
    <%= form_for(form, url: post_path(form.post), method: :patch) do |f| %>
        <% if form.errors.any? %>
            <div id="errorExplanation">
                <h2><%= pluralize(form.errors.count, "error") %> prohibited
                    this post from being saved:</h2>
                <ul>
                    <% form.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                </ul>
            </div>
        <% end %>

        <div class="field">
            <%= f.label :title %><br />
            <%= f.text_field :title, value: form.post.title %>
        </div>
        <div class="field">
            <%= f.label :content %><br />
            <%= f.text_area :content, value: form.post.content %>
        </div>
        <div class="actions">
            <%= f.submit "Update post"  %>
        </div>
    <% end %>
</div>
